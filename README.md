# 1️⃣ What is the difference between var, let, and const?

- var → function scoped, can be redeclared
- let → block scoped, can be updated
- const → block scoped, cannot be reassigned

# 2️⃣ What is the spread operator (...)?

- The spread operator is used to copy or expand arrays/objects.
  example:
  const arr = [1,2,3];
  const newArr = [...arr, 4];

# 3️⃣ What is the difference between map(), filter(), and forEach()?

- map → returns new array
- filter → returns filtered array
- forEach → just loops

# 4️⃣ What is an arrow function?

- arrow function is a short way to write function.
  example:
  const number=()=>{

}
# 5️⃣ What are template literals?
- Use backticks (``) to write strings and its help write a dynamic string
example:
<div>`hello${name}`</div>
